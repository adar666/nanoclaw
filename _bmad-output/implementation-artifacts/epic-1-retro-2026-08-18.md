---
epic: 1
date: 2026-08-18
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 1 Retrospective: Google Calendar Read/Write

## Epic summary

Three stories, all `done`, no unfinished work (`pending_stories`: none, confirmed via `sprint_status.py detect-epic --file sprint-status-google-calendar.yaml --epic 1`):

- **cal-1.2 — Create an Event on Uriel's or Devorah's Calendar** — commit `c7e716a` (#8), later extended for the single-connection pivot in `077fab7` (#10)
- **cal-1.3 — Read Your Own Calendar** — commit `1456030` (#11)
- **cal-1.5 — Update an Event on Uriel's or Devorah's Calendar** — commit `1456030` (#11, same PR as cal-1.3)

Original Stories 1.1/1.4/1.6 (cross-agent relay) are struck-through and marked obsolete in `epics-google-calendar.md` — superseded by the 2026-08-17 single-connection pivot (OneCLI only supports one Google Calendar OAuth connection per project, not per agent identity; Devorah's calendar is reached via her own Google-native sharing grant instead of a second connection). This is recorded as a pivot, not silently dropped.

Diff range for this retro: `9ef9e316..1456030d` (parent of the first calendar commit `c7e716a` through the last, via `git_evidence.py`). 4 commits, 0 merges (each PR landed as a single squash commit on `main`). One of the four (`dc72d9f8`, PR #9) is unrelated `documents.ts` image-support work interleaved between calendar PRs — excluded from this epic's analysis below, noted so file-change totals aren't misread as calendar-only.

Calendar-epic file changes: `container/agent-runner/src/mcp-tools/calendar.ts` (new, 743 lines), `calendar.test.ts` (new, 1097 lines), `container/skills/calendar/SKILL.md` (new), plus the epic's own planning artifacts (`SPEC.md`, `ARCHITECTURE-SPINE.md`, `epics-google-calendar.md`, 3 story specs, `sprint-status-google-calendar.yaml`).

**Evidence inventory**: epic spec (`epics-google-calendar.md`), architecture spine (`architecture-nanoclaw-v2-2026-08-17/ARCHITECTURE-SPINE.md`, including its own Deferred section), all 3 story specs with detailed Spec Change Logs, `sprint-status-google-calendar.yaml`, full git history for the range — all present and read. No session logs available for this run (this retrospective's own conversation is the only session record) — process-lesson analysis is drawn from the Spec Change Logs and commit messages instead, same narrowing as the previous epic's retro.

## Findings

### Aggregate views

**No god-class growth.** `calendar.ts` is 743 lines — a single cohesive domain (three tools sharing one calendar-API surface), well below `documents.ts` (2854 lines, itself flagged in the previous retro). No split recommended. **Disposition: no finding.**

**No pattern divergence.** `log()`/`ok()`/`err()` conventions match sibling `mcp-tools/*.ts` files; unlike `documents.ts`, `calendar.ts` has no special-cased header comment carving out an exception — it follows the house style throughout. **Disposition: no finding.**

**Deferred items logged in real time, not backfilled.** Unlike the documents epic (previous retro's AI-5: an orphaned `deferred-work.md` never merged into the canonical spine), this epic's deferred items (no idempotency guard, raw network-error leakage, all-day/timed PATCH mismatch, aggregate timeout bound, etc.) are recorded directly in `ARCHITECTURE-SPINE.md`'s own Deferred section as they were found — no second, unreconciled source of truth. **Disposition: no finding** — records the previous epic's process lesson being applied.

**Stale relay-model language survives in one Deferred entry.** `ARCHITECTURE-SPINE.md`'s Deferred section still describes the idempotency gap as "a relayed create and a locally-initiated create hitting the same calendar... from two different chat surfaces" — but the relay mechanism (AD-4/AD-9/AD-10) was retired by the 2026-08-17 pivot; there is no relay path anymore, only the single connected identity calling the API directly. The underlying risk (concurrent creates racing) is still real, just not via relay. Source: `ARCHITECTURE-SPINE.md`'s Deferred section, cross-checked against the pivot note in `epics-google-calendar.md:33-38`. **Disposition: fix now, low-risk doc edit** (action item AI-1).

**Cosmetic name-spelling drift in `SKILL.md`.** The spec/epics/code all consistently spell `Devorah` (and the actual `calendar` tool-argument value is the literal string `devorah`, confirmed at `calendar.ts:41`). `container/skills/calendar/SKILL.md`'s narrative prose (not the quoted argument values, which are correct) uses `Devora` in several places (frontmatter description, section headers, running text). No functional risk — every place the argument value itself is given, it's correctly quoted `"devorah"`. **Disposition: accept as-is**, optional cosmetic cleanup (action item AI-2, low priority).

**Spec-to-implementation reconciliation: clean.** FR1–FR3 all map to shipped tools; the pivot's AD retirements (AD-4/AD-9/AD-10) are recorded in place rather than silently deleted; obsolete Stories 1.1/1.4/1.6 are struck through with a superseded-by note rather than removed from the record. No undocumented gap found.

### Cross-story boundary review

Checked specifically because no single story's own review session ever saw both sides of a seam:

- **cal-1.2 → cal-1.3/cal-1.5 shared plumbing**: `CALENDAR_IDS`, `TIMEZONE`/`parseZonedToUtc`, `extractSetupUrl`, `notConnectedMessage`, and the TLS shim are all reused unmodified — confirmed by reading `calendar.ts` directly, no duplicated mapping or re-implemented gateway-error parsing.
- **cal-1.3 → cal-1.5 shared search/formatting**: `update_calendar_event` (`calendar.ts:554-743`) calls the same `searchEvents`, `formatEventLine`, and `formatRangeDesc` helpers `list_calendar_events` defines — confirmed at `calendar.ts:658` (`searchEvents(...)`), `:661` (`formatRangeDesc(...)`), `:674` (`formatEventLine(...)`). No hand-rolled duplicate formatting, matching what both story specs' own Code Maps required. **Clean, no finding.**
- **Error-shape consistency**: all three tools return the identical `ok()`/`err()` shape; gateway-error handling (AD-8) is identical across all three, confirmed by direct read rather than by story-spec claim alone. **Clean, no finding.**

### Behavior verification

Ran directly in this retro session (not just re-reading prior reports):

- `cd container/agent-runner && bun test` — 390 pass, 8 skip, 0 fail, matching every story's own reported count exactly. `calendar.test.ts` alone: 71 pass, 0 fail.
- `pnpm exec tsc -p container/agent-runner/tsconfig.json --noEmit` — clean, no errors.

**Live end-to-end verification gap.** Story cal-1.2's own Acceptance Criteria required, and its Spec Change Log documents, a real live call: `calendar: 'uriel'` created a real event with a real `htmlLink`; `calendar: 'devorah'` correctly failed with a real Google `403 requiredAccessLevel` before she'd shared her calendar. Stories cal-1.3 and cal-1.5 each declared the identical class of acceptance criterion — cal-1.3: *"real events come back with correct details — verify live once implemented"*; cal-1.5: *"the change lands correctly on the real calendar... verify live once implemented"* — but neither story's Spec Change Log nor the PR #11 commit message records that this happened; both report only the mocked-`fetch` test suite and `tsc`. This retro session has no live Google Calendar credentials either, so it could not close the gap directly. Source: `spec-cal-1-3-...md`'s Change Log (no live-verification line), `spec-cal-1-5-...md`'s Change Log (same), PR #11 commit message (same). **Disposition: fix now** (action item AI-3) — the risk is bounded (both tools reuse cal-1.2's already-live-proven fetch/timezone/TLS-shim plumbing verbatim), but the specific acceptance criterion each story wrote for itself was never actually closed out.

## Previous-retro follow-through

This project tracks each feature epic in its own `sprint-status-<feature>.yaml` file rather than one shared file — `sprint-status-google-calendar.yaml` has no prior epic and no prior retro of its own; this is the first. `--set-action-status` is not applicable here.

As an aside (different tracker, different epic — not part of this retro's formal follow-through, no status write proposed): the *documents* epic-1's retro (`epic-1-retro-2026-08-16.md`, tracked in the main `sprint-status.yaml`) left 8 action items. Spot-checked against the current codebase while gathering evidence for this run: AI-1 (log() calls), AI-2 (raw-file frontmatter field), AI-4 (Hebrew font stack documented), AI-5 (deferred-work.md merged into the spine), AI-6 (AD-7 recorded as accepted), AI-7 (spec-1-2 Change Log backfilled), and AI-8 (`stripControlChars` on fill's `value`) all show real evidence of landing in the code/docs. AI-3 (SPEC.md's "summary entry" wording vs. the mechanical-filename reality) still shows no reconciliation in `spec-document-memory/SPEC.md`. This is offered as context only, not acted on here.

## Action items

All proposed here for human decision — this retrospective does not apply code or spec changes itself.

| ID | Item | Owner | Evidence |
|----|------|-------|----------|
| AI-1 | Reword `ARCHITECTURE-SPINE.md`'s Deferred-section idempotency bullet to drop the stale "relayed create" phrasing (relay model retired by the pivot) — keep the underlying concurrent-create risk, just not attributed to a mechanism that no longer exists | spec owner | `ARCHITECTURE-SPINE.md` Deferred section vs. `epics-google-calendar.md:33-38`'s pivot note |
| AI-2 | Align `SKILL.md`'s narrative prose spelling ("Devora" → "Devorah") with spec/epics/code convention — cosmetic only, actual tool-argument values are already correct everywhere quoted | spec owner | `container/skills/calendar/SKILL.md` (prose) vs. `calendar.ts:41` (`devorah` key) and `epics-google-calendar.md` (consistently "Devorah") |
| AI-3 | Run one live `list_calendar_events` call and one live `update_calendar_event` call against the real connected calendar (either person), and record the confirmation (a Spec Change Log line, same precedent as cal-1.2's) — closes the "verify live once implemented" AC both stories wrote for themselves and never checked off | dev | `spec-cal-1-3-...md` / `spec-cal-1-5-...md` Change Logs (no live-verification entry) vs. their own Acceptance Criteria; PR #11 commit message (mock-test-only report) |

**Process lesson**: none new to name — this epic applied the previous epic's lesson (use the Spec Change Log for every implementation-time deviation, not just a code comment) consistently across all three stories, each with a detailed, well-sourced entry. Worth recording as confirmation the mechanism is working, not as a new gap.

## Acceptance verdict

**accepted-with-open-items**

Criteria: **declared** — each story's own Acceptance Criteria (FR1/FR2/FR3 traced through `epics-google-calendar.md`'s FR Coverage Map), plus the epic's live-verified pivot note for cal-1.2 (real event created, real `htmlLink`, real Devorah-403 confirmed before sharing).

Met: all 3 stories `done`, no unfinished work. Full test suites green (container: 390/8/0, re-run independently in this session) and typecheck clean (re-run independently). Cross-story boundary review found clean, correctly-factored shared plumbing with no duplication or divergence. No god-class growth. Deferred items properly tracked in the canonical spine, applying the previous epic's own process lesson.

Not a plain **accepted**: 3 named, tracked open items remain (1 real live-verification gap — AI-3 — plus 2 low-risk doc/cosmetic fixes — AI-1, AI-2), each with a source reference above, none blocking what shipped — that's exactly what accepted-with-open-items exists for.

## Open questions

- AI-3's live verification needs a moment with real Google Calendar access (either person's) — worth doing before this feature sees more real-world use, not urgent enough to block anything already shipped, since cal-1.3/cal-1.5 reuse cal-1.2's already-live-proven fetch/timezone/TLS-shim plumbing verbatim.
- The unrelated documents-epic's still-open AI-3 (SPEC.md "summary entry" wording) is a standing product-scope decision from 2026-08-16, unrelated to this epic — flagged above only as context, not something this retro resolves.

## Assumptions

Not applicable — this run was interactive, not headless. Every material call (epic detection, verdict, action items) is recorded above in its own section rather than here.
